# only for compatibility with old python and pip
from setuptools import setup

setup(dependencies = [
    "holoviews",
    "bokeh",
    "pandas",
    "panel",
    "pyadic",
    "mpmath"
],
name = "bella",
version = "0.0.99"
)
